package Gobstones

trait Console extends App with CommonLanguage {
  
  def imprimir(x: Any) = println(x)
  
  def probar(msg: String, p: Boolean) = imprimir(msg + ": " + p)
  
  def main()
  
  main()

}